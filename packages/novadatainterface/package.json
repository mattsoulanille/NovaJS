{
  "name": "novadatainterface",
  "version": "0.1.6",
  "description": "An interface for game data for NovaJS",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "scripts": {
    "build-defaults": "ts-node --esm scripts/pack_png.ts defaults/default_pict.png src/default_pict.ts && ts-node --esm scripts/pack_png.ts defaults/default_rled.png src/default_rled.ts",
    "build": "npm run build-defaults && tsc",
    "clean": "rimraf dist",
    "test": "jasmine --config=jasmine.json"
  },
  "dependencies": {},
  "devDependencies": {
    "@evn/tsconfig": "0.1.0",
    "@types/argparse": "^2.0.10",
    "@types/jasmine": "^5.1.13",
    "argparse": "^2.0.1",
    "jasmine": "^5.12.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "5.9.3"
  }
}
