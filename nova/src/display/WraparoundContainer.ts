import * as PIXI from "pixi.js";

class WraparoundContainer extends PIXI.Container {
    constructor(private bound: number) {
        super();
    }

    //    setPos({ x, y }: { x: number, y: number }) {

    //    }


}
