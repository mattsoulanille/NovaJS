{
  "name": "novadatainterface",
  "version": "0.0.1",
  "description": "An interface for game data for NovaJS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --declaration --outDir . --emitDeclarationOnly"
  },
  "author": "Matthew Soulanille",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.1.6"
  }
}
